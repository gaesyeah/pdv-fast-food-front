import { StyledTitle, WalletIcon } from "./styles";

const Title = () => {
  return (
    <StyledTitle>
      <WalletIcon />
      <h2>Pagamento:</h2>
    </StyledTitle>
  );
};

export default Title;
